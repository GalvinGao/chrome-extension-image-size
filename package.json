{
  "name": "chrome-extension-image-size",
  "version": "1.5.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check content.js && node --check sizes.js && node --check popup.js && node --check settings.js"
  }
}
